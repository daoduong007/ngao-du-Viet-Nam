import React from 'react';

import { StyledAppbar, Logo, Directional } from '@components';

export const Appbar = () => {
  return (
    <StyledAppbar>
      <Logo />
      <Directional />
    </StyledAppbar>
  );
};
